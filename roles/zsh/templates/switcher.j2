# check if switcher is installed
if ! command -v switcher &> /dev/null
then
    echo "switcher could not be found"
else
    source <(switcher init zsh)
    alias s=switch
    alias sl="switcher list-contexts"
    source <(compdef _switcher switch)
fi
